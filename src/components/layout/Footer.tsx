export default function Footer() {
  return (
    <footer className="border-t border-teal/10 py-8">
      <div className="mx-auto max-w-6xl px-6 font-mono text-xs text-teal-dark/50">
        ORATO — runs entirely in your browser. Nothing you record leaves your device.
      </div>
    </footer>
  );
}
